# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Virtualenvs / package caches ---
.venv/
venv/
.uv_cache/
node_modules/

# --- Build / synth output ---
cdk.out/
dist/
build/
.next/

# --- Tooling caches that do not belong in the repo ---
graphify-out/

# --- Secrets & local config ---
.env
.env.*
!.env.example
*.pem
*.key

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp

# --- Scratch ---
screenshot.png

# --- Vendored agent-skills library ---
# Lives outside the project at S:/ECC (clone of github.com/affaan-m/ECC).
# Kept out of the repo: nothing here references it, and its own .agents/ and
# .claude/ trees would otherwise pollute skill discovery for this project.
ECC/
